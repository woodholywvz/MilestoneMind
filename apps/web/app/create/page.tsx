import { CreateDealForm } from "../../src/components/CreateDealForm";

export const dynamic = "force-dynamic";

export default function CreateDealPage() {
  return (
    <main className="page-shell">
      <CreateDealForm />
    </main>
  );
}
