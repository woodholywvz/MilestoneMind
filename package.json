{
  "name": "milestone-mind",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "packages/shared",
    "services/executor",
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @milestone-mind/web",
    "dev:ai": "node ./scripts/run-ai-service.mjs",
    "dev:executor": "npm run dev --workspace @milestone-mind/executor",
    "build": "npm run build --workspace @milestone-mind/shared && npm run build --workspace @milestone-mind/executor && npm run build --workspace @milestone-mind/web",
    "lint": "npm run lint --workspace @milestone-mind/shared && npm run lint --workspace @milestone-mind/executor && npm run lint --workspace @milestone-mind/web",
    "test": "npm run test --workspace @milestone-mind/shared && npm run test --workspace @milestone-mind/executor && node ./scripts/check-ai-health-route.mjs",
    "anchor:build": "anchor build"
  },
  "devDependencies": {
    "@eslint/js": "^9.24.0",
    "@types/node": "^24.0.0",
    "eslint": "^9.24.0",
    "globals": "^16.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "typescript-eslint": "^8.29.0"
  }
}
